# Default duplicate handling

Default duplicate handling

## Usage

``` r
values_fn_default(x)
```

## Arguments

- x:

  vector to be summarized into a single value of the same type

## Value

one-dimensional vector of the same type as `x`
