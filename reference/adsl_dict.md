# create adsl dictionary from column labels/names

create adsl dictionary from column labels/names

## Usage

``` r
adsl_dict(data, param = "param", label = "label")
```

## Arguments

- data:

  adsl-like data set

- param, label:

  column name for resulting tibble where column name (param) and label
  are stored

## Value

a `tibble`
