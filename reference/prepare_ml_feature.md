# prep feature matrix

prep feature matrix

## Usage

``` r
prepare_ml_feature(feature, vars_fct_expl_na = NULL, level_other = "other")
```

## Arguments

- feature:

  feature tibble

- vars_fct_expl_na:

  defaults to `NULL`

- level_other:

  defaults to 'other'

## Value

updated feature matrix

## Details

**\[deprecated\]** Deprecated since update in
[`prepare_ml()`](https://bayer-group.github.io/bmdi-mlai-martini/reference/prepare_ml.md)
as of martini 0.6.0
