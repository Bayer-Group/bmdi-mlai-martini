# Required spec entry by data type

used internally for "use_for_build" check, as well as in the
documentation to help the user spot issues with spec entries

## Usage

``` r
spec_cols_required
```

## Format

A list with top level elements for each data type, giving the set of
required entries for that data type.
