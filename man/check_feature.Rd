% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_feature.R
\name{check_feature}
\alias{check_feature}
\title{check feature matrix}
\usage{
check_feature(
  x,
  check_low_freq = TRUE,
  check_other = TRUE,
  check_missing = TRUE,
  check_count = TRUE,
  check_nzv = TRUE,
  quiet = FALSE,
  thres_count = 30,
  thres_low_freq = NULL,
  thres_missing = NULL,
  thres_nzv_freq = NULL,
  thres_nzv_unique = NULL,
  ...
)
}
\arguments{
\item{x}{feature matrix to check, such as the output of \code{\link[=build]{build()}}.}

\item{check_low_freq, check_other, check_missing, check_count, check_nzv}{logicals to control which checks to include. All default to TRUE.}

\item{quiet}{logical controlling whether any informative messages are
printed to the console. Defaults to FALSE.}

\item{thres_count}{passed to  \code{\link[=check_count]{check_count()}}}

\item{thres_low_freq}{passed to \code{\link[=check_freq]{check_freq()}}}

\item{thres_missing}{passed to \code{\link[=check_non_missing]{check_non_missing()}}}

\item{thres_nzv_freq, thres_nzv_unique}{passed to \code{\link[=check_nzv]{check_nzv()}}}

\item{...}{arguments to be passed to other methods}
}
\value{
a nested list, where each entry has a \code{vars} entry that contains
column names that do not pass the respective check (if check is included),
character of length 0 for empty sets.
\code{NULL} if not tested at all.
}
\description{
\code{check_feature()} is by default run in \code{\link[=prepare_ml]{prepare_ml()}} on the
input \code{feature} to notify the user on sources of potential issues.
}
\examples{
# check_feature() on martini_feat (package data, few issues expected)
check_feature(martini_feat)

# Synthetic data set designed to trigger all checks:
n <- 100
feat_issues <- tibble::tibble(
  .id = 1:n,
  # two levels with n=1, below default threshold
  low_freq   = factor(c(rep("common", n - 2), "rare1", "rare2")),
  # 99/100 values identical -> near-zero variance
  near_const = c(1, rep(2, n - 1)),
  # 25\% NAs, below thres_imp = 0.8
  high_miss  = c(rep(NA, 25), runif(n - 25)),
  # non-negative integers, only 3 distinct values
  count_var  = sample(1:3, n, replace = TRUE),
  # already contains the lumping class "other_ml"
  has_other  = factor(c(rep("A", n - 1), "other_ml"))
)
check_feature(feat_issues)
check_freq(feat_issues)
check_other_class(feat_issues)
check_non_missing(feat_issues)
check_nzv(feat_issues)
check_count(feat_issues)

}
